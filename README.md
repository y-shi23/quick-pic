# Quick Pic - 快速图片处理工具

一个基于 TypeScript 的快速图片处理工具,可以为图片添加蒙版、调整透明度和模糊效果。

## 功能特性

- **蒙版效果**: 为图片添加自定义颜色的蒙版层,可调节透明度(0-100%)
- **透明度调整**: 调整图片整体透明度(0-100%)
- **模糊效果**: 添加高斯模糊效果(0-20px)
- **拖拽上传**: 支持拖拽上传和点击上传两种方式
- **实时预览**: 所有调整实时显示在画布上
- **下载功能**: 支持下载处理后的图片为 PNG 格式
- **现代化界面**: 深色主题,毛玻璃效果,流畅动画

## 快速开始

### 安装依赖
```bash
npm install
```

### 启动开发服务器
```bash
npm run dev
```

服务器将在 http://localhost:3000 启动

### 构建生产版本
```bash
npm run build
```

## 部署到 Cloudflare Pages

### 方法一: 使用 Wrangler CLI（推荐）

1. 安装 Wrangler
```bash
npm install -g wrangler
```

2. 登录 Cloudflare
```bash
wrangler login
```

3. 构建项目
```bash
npm run build
```

4. 部署到 Cloudflare Pages
```bash
wrangler pages deploy dist --project-name=quick-pic
```

### 方法二: 通过 Cloudflare Dashboard

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
2. 进入 **Pages** 页面
3. 点击 **Create a project**
4. 连接你的 Git 仓库（GitHub/GitLab）
5. 配置构建设置:
   - **构建命令**: `npm run build`
   - **构建输出目录**: `dist`
   - **Node 版本**: 18 或更高
6. 点击 **Save and Deploy**

### 方法三: 使用 Git 自动部署

1. 将代码推送到 GitHub/GitLab
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push -u origin main
```

2. 在 Cloudflare Pages 中连接仓库,每次推送都会自动部署

## 使用说明

1. **上传图片**: 拖拽图片到上传区域或点击选择图片
2. 使用控制面板调整各项参数:
   - 🎭 **蒙版透明度**: 控制蒙版的不透明程度
   - 🎨 **蒙版颜色**: 选择蒙版的颜色
   - 💧 **图片透明度**: 调整原图的透明度
   - 🌫️ **模糊程度**: 设置图片的模糊程度
3. 🔄 点击**重置**按钮恢复所有设置
4. ⬇️ 点击**下载图片**保存处理后的图片

## 技术栈

- TypeScript
- HTML5 Canvas
- Vite
- CSS3
- Cloudflare Pages

## 项目结构

```
quick-pic/
├── index.html          # 主页面
├── package.json        # 项目配置
├── tsconfig.json       # TypeScript 配置
├── vite.config.ts      # Vite 配置
├── wrangler.toml       # Cloudflare 部署配置
└── src/
    ├── main.ts         # 主要逻辑和入口
    └── style.css       # 样式文件
```

## 许可证

MIT
